{
  "name": "@autonomy/conductor",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "clean": "rm -rf node_modules .turbo"
  },
  "dependencies": {
    "@autonomy/shared": "workspace:*",
    "@autonomy/agent-manager": "workspace:*",
    "@pyx-memory/client": "workspace:*"
  }
}
